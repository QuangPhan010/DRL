# Backend API
Django>=5.2,<6.0
djangorestframework>=3.16,<4.0
django-cors-headers>=4.7,<5.0
drf-yasg>=1.21.10,<2.0

# Spreadsheet and transcript imports
openpyxl>=3.1.5,<4.0
pdfplumber>=0.11.7,<0.12
PyMuPDF>=1.25,<2.0

# Vietnam timezone handling used by attendance windows
pytz>=2025.1

# Face ID runs in the browser through @vladmandic/human (frontend/package.json),
# so it does not require a Python ML package here.
